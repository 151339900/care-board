<div class= "container px-5 px-sm-0">
 <h2 class= "text-secondary">投稿一覧</h2>
  <table class="table table-striped">
    <tr>
      <td class= "text-secondary">タイトル</td>
      <td class= "text-secondary">ニックネーム</td>
      <td class= "text-secondary">ジャンル名</td>
      <td class= "text-secondary">コメント数</td>
    </tr>
    <% @posts.each do |post| %>
      <tr>
        <td><%= link_to post.title, post_path(post.id,), class:"link" %></td>
        <td><%= link_to post.user.nick_name, "/users/#{post.user_id}", class:"link" %></td>
        <td class= "text-secondary"><%= post.genre.name %></td>
        <td><%= link_to "#{post.comments.count} コメント", post_path(post.id), class:"link" %></td>
      </tr>
    <% end %>
  </table>
</div>

<h2>プロフィール編集</h2>
<%= form_with model: @user, url: user_path(@user.id) do |f| %>
  <label for="inputName">Name</label>
  <%= f.text_field :nick_name, autofocus: true, id:"inputName", placeholder:"名前"%>
  <%= f.text_area :introduction, autofocus: true, rows: 5 %>
  <%= f.submit %>
<% end %>